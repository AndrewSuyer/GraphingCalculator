// Entry point for executable 'Calculator' in /GraphingCalculator/build/Debug/src/Calculator
// When changes are made, run :CMakeBuild (in neovim) then 'make' from inside the build directory to make executable

#include "calculator/Calculator.h"

int main()
{
    Calculator c;

    return 0;
}
